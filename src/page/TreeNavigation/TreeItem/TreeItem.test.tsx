import React from 'react';
import { shallow } from 'enzyme'
import { TreeItem } from './TreeItem'

describe('TreeItem component', () => {
    it('should', () => {
        // setup
        // const treeitem = shallow(<TreeItem  prop="prop" />);
        // test
        // expect(treeitem.text()).toEqual('');
    });
})