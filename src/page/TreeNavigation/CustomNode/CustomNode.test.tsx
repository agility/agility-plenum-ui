import React from 'react';
import { shallow } from 'enzyme'
import { CustomNode } from './CustomNode'

describe('CustomNode component', () => {
    it('should', () => {
        // setup
        // const customNode = shallow(<CustomNode  prop="prop" />);
        // test
        // expect(customNode.text()).toEqual('');
    });
})