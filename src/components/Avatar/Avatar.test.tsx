import React from 'react';
import { shallow } from 'enzyme'
import { Avatar } from './Avatar'

describe('Avatar component', () => {
    it('should', () => {
        // setup
        // const avatar = shallow(<Avatar  prop="prop" />);
        // test
        // expect(avatar.text()).toEqual('');
    });
})