import React from 'react';
import { shallow } from 'enzyme'
import { InputLabel } from './InputLabel'

describe('InputLabel component', () => {
    it('should', () => {
        // // setup
        // const inputlabel = shallow(<InputLabel  prop="prop" />);
        // // test
        // expect(inputlabel.text()).toEqual('');
    });
})