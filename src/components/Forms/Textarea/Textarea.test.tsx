import React from 'react';
import { shallow } from 'enzyme';
import { Textarea } from './Textarea';

describe('Textarea component', () => {
    it('should', () => {
        // setup
        // const textarea = shallow(<Textarea  prop="prop" />);
        // test
        // expect(textarea.text()).toEqual('');
    });
});
