import React from 'react';
import { shallow } from 'enzyme';
import { Combobox } from './Combobox';

describe('Combobox component', () => {
    it('should', () => {
        // // setup
        // const combobox = shallow(<Combobox  prop="prop" />);
        // // test
        // expect(combobox.text()).toEqual('');
    });
});
