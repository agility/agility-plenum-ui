import React from 'react';
import { shallow } from 'enzyme'
import { Switch } from './Switch'

test('Switch component', () => {
    // setup
    // const switch = shallow(<Switch  prop="prop" />);
    // // test
    // expect(switch.text()).toEqual('');
})