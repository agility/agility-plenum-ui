import React from 'react';
import { shallow } from 'enzyme'
import { TreeView } from './TreeView'

describe('TreeView component', () => {
    it('should', () => {
        // setup
        // const treeview = shallow(<TreeView  prop="prop" />);
        // // test
        // expect(treeview.text()).toEqual('');
    });
})